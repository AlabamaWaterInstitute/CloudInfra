name: Manual Build and Push Docker image to Docker Hub
on:
  workflow_dispatch:
jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
      - uses: actions/checkout@v2
      - uses: ./.github/action_templates
